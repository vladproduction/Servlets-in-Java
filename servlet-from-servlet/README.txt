project done as a simple example of invoke the one servlet from another;
 1)when include = logic of the code ServletA + logic of ServletB
 2)when forward = only logic of the code ServletB

 sample pack:
 -by DispatcherServlet we organize which of Servlet should be done;

 listener

 redirect (by class ServletC)
